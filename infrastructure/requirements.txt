# CDK app dependencies (Step 1.14).
#
# Installed into the project .venv:
#   .venv/Scripts/python -m pip install -r infrastructure/requirements.txt
#
# These are infra-only and intentionally separate from the application's
# runtime dependencies in pyproject.toml.

# PINNED EXACTLY for reproducible CD. The deploy runner does a clean
# `pip install`, so floating versions drift and break: cdk-nag 3.0 removed
# `NagSuppressions`, and a floating aws-cdk-lib emitted cloud-assembly schema
# 54.0.0 which outran the CDK CLI pinned in .github/workflows/deploy-*.yml.
# aws-cdk-lib's schema MUST be readable by that CLI -- bump these and the CLI
# together, then re-validate with `python app.py` in .venv-cdk.
#   aws-cdk-lib 2.259.0 -> assembly schema 54.0.0 -> needs CDK CLI >= 2.1126.0
aws-cdk-lib==2.259.0
constructs==10.6.0
cdk-nag==2.38.2
